//js
// 函数声明
function sum(x, y) {
    return x + y;
}
// 函数表达式
var mySum = function (x, y) {
    return x + y;
};
//TS
function sumTS(x, y) {
    return x + y;
}
var sumTScript = function (x, y) {
    return x + y;
};
var sumTtypeScript = function (x, y) {
    return x + y;
};
sumTS(1, 2);
// sumTS(1,true)
// 参数的数量同样有显示
// sumTS(1,2,3);
